# PR マージスケジュール

最終更新: 2026-08-23（オープンPR10本を全数ビルド実測で再構成／08-09〜08-23のマージ17件を完了済みへ反映／#415・#414・#362の未マージクローズを記録／突合中に見つかった main 現存バグ3件は本PR #436 で修正済み）

> **⚠️ 予定表が実態からズレることについて（2026-08-23）**: 前回更新（08-09）から2週間で、A/B表の予定PRは**ほぼ全てが予定日と無関係にマージまたはクローズ**され、表だけが08-09のまま残っていた。日付入りの予定表は放置すると「期限切れの指示」として判断を誤らせるため、**日付は着手時に決める運用に切り替え、本表からは固定の予定日を外した**。<br>また旧注記が参照していた PR #388（本ファイルの全面リライト案）は既にクローズ済みのため、当該注記は削除した。

> **判定基準**（CLAUDE.md「運用の大原則」準拠）
> - 🟢 **安全**: 構造ファイル無変更・20ファイル未満・共通コンポーネント連打なし → 個別ページ爆発半径・日次OK
> - 🟡 **要観察**: 共通コンポーネント（Navbar等）に触れる or prerender/sitemap に追記 → 単独・7日空けて次
> - 🔴 **マージ不可**: 20ファイル超の一括 or 構造＋共通コンポーネント → 分割が必要
>
> 構造ファイル = prerender.ts / useMeta.ts / urlMap.ts / sitemap.xml / robots.txt / _redirects / seoDate.ts / hreflang / canonical

## デプロイ頻度の判断基準（波及数 × ローリング7日）

**大原則: 「24時間に1回」は絶対法則ではない。効いているのは時計ではなく「波及ページ数」と「触ったレバー」の2つだけ。**

Googleが再評価するのは `dist/` の静的HTML。判断はソースのファイル数ではなく、**ビルド後にHTMLが変わるページが何枚あるか（＝出力の波及数）**で決める。

- `Footer.tsx` を1ファイル直しただけでも全ページが使っていれば**全ページのHTMLが変わる＝全サイト更新シグナル**。「1ファイルだから安全」は誤り。
- 逆に個別ページ20枚をそれぞれ直しても波及は各ページに閉じる。
- CSS/classNameも同じ。共通コンポーネントの className を変えれば全ページのHTMLが変わる（5/11 grid-cols 65ファイル一括 → +15日で外免切替 -3位の実証）。

### レバー × 波及数 → 頻度

| 触るレバー | 波及ページ | 同日の頻度 | 観察 |
|-----------|-----------|-----------|------|
| **Level 4・個別ページ**（本文・新規ページ・内部リンク・画像/alt・1ページ限定のUI/CSS） | 1〜数 | **1日2回OK**（合計20ページ未満・同じ共通部品を連打しない） | 不要 |
| **Level 4だが大量**（20ページ以上を一括） | 20+ | 1日1回・バースト禁止 | 7日 |
| **Level 3**（title・H1・meta description・グローバルナビリンク） | 1〜2推奨 | 1日1回・1〜2ページまで | **1週間**空けて次 |
| **共通コンポーネント**（Header/Footer/Hero/Nav/Pricing/WhyUs/FAQ/CardGrid 等）※ファイル数不問 | **全ページ** | 1回入れたら止める | **7日**・その窓で他の共通部品を出さない |
| **構造**（hreflang/canonical/sitemap/robots/_redirects/URL/ドメイン/prerenderルート/useMeta/urlMap/seoDate/JSON-LD） | **全サイト＋遅延余震** | 単独で1回・他と絶対混ぜない | **7〜14日**・他の構造変更を凍結（※Googleの再評価自体は2〜4週間かかるため、順位への遅延影響の観察は引き続き最低14日必要。ここでの日数は「次の大変更まで空けるマージ間隔」のみを指す） |

### 客観的に測る（推測しない）

`npm run build` 後、`dist/` の `*.html` を main と比較して**変わったHTMLファイル数**を数える＝波及ページ数。< 20 かつ共通コンポーネント/構造レバー非該当なら Level 4・日次OK。20以上 or 共通部品 or 構造なら「大変更」。

> **⚠️ 必ずJSバンドルハッシュを正規化してから数えること（誤測定の最大要因）**
>
> ソースを1行でも変えると vite のバンドル名（`/assets/js/index-XXXXXXXX.js`）が変わり、**全ページのHTMLに1行だけ差分が入ります**。素朴に `cmp` で数えると、実波及1ページの変更が **132ページ**に見えます（#386・2026-08-09 実測）。この誤測定は🟢を🔴に見せるため、判断を根本から狂わせます。過去の #359 で「JSバンドルハッシュ差分131ページは無害ノイズ」と記録されているのと同じ現象です。
>
> 正しい手順（main と対象ブランチの両方をビルドし、`dist_main` を退避してから実行）:
>
> ```python
> import re, pathlib
> pat = re.compile(r'(/assets/(?:js|css)/[A-Za-z0-9._-]*?-)[A-Za-z0-9_-]{8}(\.(?:js|css))')
> norm = lambda p: pat.sub(r'\1HASH\2', p.read_text(encoding='utf-8', errors='replace'))
> base, new = pathlib.Path('dist_main'), pathlib.Path('dist')
> real = [str(f.relative_to(new)) for f in sorted(new.rglob('*.html'))
>         if not (base / f.relative_to(new)).exists() or norm(base / f.relative_to(new)) != norm(f)]
> print('実波及ページ数:', len(real)); [print('  ', r) for r in real]
> ```
>
> シェルの `sed 's|...|...|'` は区切り文字 `|` が正規表現の選択 `(js|css)` と衝突して**黙って失敗し、全ファイルが空になって「波及0」という嘘の結果**を返します。必ず上記のPythonを使うこと。

### ローリング7日バケツ（累積で見る・構造と共通コンポーネントは同じバケツ）

**（2026-07-23 ユーザー承認で14日→7日に短縮）マージ間隔（次の大変更まで空ける日数）は7日に短縮。ただし構造・大量・共通部品の影響がGoogle側で2〜4週間遅れて出る（余震）という実測知見自体は変わらないため、「順位への遅延影響を観察する期間」は引き続き最低14日を推奨する。7日はあくまで次の大きい変更をマージしてよいまでの間隔であり、直前の変更の影響がまだ完全には出きっていない可能性を踏まえた運用判断であることに注意。**

だから危険なのは1回の頻度より**直近7日の累積シグナル量**。**「共通コンポーネント」と「構造ファイル」は別トラックではなく同じ1つのローリング窓を共有する**——構造を入れた3日後に共通コンポーネントを入れる、は「7日以内に2回大きい注ぎ」に該当し禁止。

- **固定バケツではなくスライドするバケツ**: 毎日「今日から遡って7日」を見る。大きい変更前に必ず「過去7日に大きいシグナルを出してないか？」を確認。
- **バケツに入る量は回数ではなく波及の大きさ**: 個別ページ本文＝数滴💧／20ページ超＝大量🪣／共通部品＝一杯／構造＝溢れる＋排水が遅い。
- **ルール**: スライド窓の中に「大きい注ぎ」（構造 or 共通コンポーネント or 20超一括、種類を問わず）は1回まで。数滴（個別コンテンツ）は毎日足しても溢れない。

## 完了済み（git log 実測・日付はすべて `origin/main` のコミット日で確認済み）

| 日付 | PR | 内容 |
|------|----|------|
| 2026-05-21 | #280 | referral_source フィールド追加（全5フォーム） |
| 2026-05-23 | #268 | カードレイアウト統一（WhyUs/PainPoints/Services/CaseStudies） |
| 2026-06-28 | #301 | #273（開発ツール）の本番revert＋回復期ルール書き直し |
| 2026-06-28 | #287 | NBI 更新・再発行プラン 郵送料 +7,000→+6,000 修正 |
| 2026-06-28 | #300 | Contact(EN) WhatsApp主役化・「email only」撤去 |
| 2026-06-28 | #243 | PSA料金表追加（重複e-cert警告ブロックは除去済み） |
| 2026-07-05 | #288 | フィリピン書類の日本語翻訳サービスページ追加（`/ja/honyaku/`・純増） |
| 2026-07-08 | #307 | useMeta hreflang全削除バグ修正 |
| 2026-07-11 | #326 | main マージ前 必須チェックプロトコル＋デプロイ頻度判断基準（docs） |
| 2026-07-11 | #306 | PRC看護師書類のB2B戦略（docs） |
| 2026-07-11 | #303 | HomeJa 自己掲載 review[] 除去 |
| 2026-07-11 | #327 | main マージ前チェック強制フック（`.claude/`） |
| 2026-07-11 | #328 | スケジュール自動マージ例外＋カレンダー（docs） |
| 2026-07-14 | #325 | title/desc同期バッチ1・8ページ |
| **2026-07-16** | #308 | 5ページの共通コンポーネントprop名誤り修正（FAQ/CTA空表示解消） |
| **2026-07-16** | #311 | 問い合わせフォーム死にリード対策（メール検証＋確認欄＋予備連絡先） |
| **2026-07-16** | #320 | Home中盤CTA＋HomeJa警告箱リカラー（#305分割） |
| **2026-07-17** | #352 | スケジュールをコンテンツ優先に組み替え（docs） |
| **2026-07-17** | #329 | PSA e-Certificate 3ページのh1欠落修正＋整合性監査レポート |
| **2026-07-17** | #330 | NBIページ2枚にトラストブロック追加 |
| **2026-07-17** | #333 | 帰化書類パック総額シミュレーター追加 |
| **2026-07-17** | #336 | PSA OTP解説＋FAQ構造化データ |
| **2026-07-17** | #337 | 問い合わせフォーム構造化5項目(JA) |
| **2026-07-17** | #338 | 薄ページ対策（FAQ修正5＋本文加筆5） |
| **2026-07-17** | #341〜#348 | title/desc同期 バッチ2〜9（EN/JA主要サービス・結婚ビザ・国別・情報系・事業者向け、静的HTML波及ほぼ0） |
| **2026-07-19** | #356 | PSA物理アポスティーユ廃止の事実訂正のみ（価格は現状維持）※詳細は下記「注意」参照 |
| **2026-07-19** | #355 | PSA e-Cert/e-Apostille 実費・納品パターン・本人確認・決済の加筆 |
| **2026-07-19** | #334 | e-Apostille不可の解説ページ新規追加（構造ファイル追記） |
| **2026-07-19** | #335 | データセキュリティ方針ページ3言語新規追加（構造ファイル追記） |
| **2026-07-20** | #369 | docs/merge-schedule.md 全面リライト（A/B/C表構造・実測ベース再構築、波及0） |
| **2026-07-20** | #359 | PSA e-Certificate 本人確認（代理可否）の記述訂正（自動マージ実測波及1ページ・JSバンドルハッシュ差分131ページは無害ノイズと確認） |
| **2026-07-22** | #366 | e-Apostille事実訂正 バッチ1/2（自動マージ実測波及15ページ・想定通り一致） |
| **2026-07-23** | #367 | e-Apostille事実訂正 バッチ2/2（自動マージ実測波及13ページ・想定通り一致） |
| **2026-07-23** | #374 | 単品PSA書類の認証込みプランを物理アポスティーユ→e-Apostille（電子）に修正（¥61,000→¥30,000/+¥10,000で¥40,000）。7項目カード判定🟡（実測波及1ページのみだがPricingは共通コンポーネント名指し・07-19凍結期間中）をユーザーに提示し、「事実誤り（提供不可能なサービス表示）の是正」という緊急性を理由に「main merge」明示入力で凍結を上書きしてマージ |
| **2026-07-25** | #378 | マージ間隔ルールを14日→7日に短縮（CLAUDE.md・docs/merge-schedule.md、波及0・ユーザー明示指示） |
| **2026-07-25** | #379 | LLMO 1/3: entity sameAs＋llms.txt公式チャネル（#363の混入除去リベース版。実測波及3ページ＋静的3ファイル・想定通り一致） |
| **2026-07-26** | #364 | LLMO 2/3: ページ別構造化データ（speakable/ItemList/HowTo/DefinedTermSet）。自動マージ実測波及6ページ（A表記載の4ページより多いが同一カテゴリの個別ページJSON-LDのみと確認） |
| **2026-07-28** | #376 | 問い合わせフォーム死にリード対策バグ修正＋送信前PaymentTrust表示。自動マージ実測波及2ページ（en/contact・ja/contact。ko/contactはJS挙動のみの修正でSSR差分なし・想定通り） |
| **2026-07-29** | #319 | WhyUs 4枚目カード＋Footer/FooterKo フォーム摩擦削減（共通コンポーネント・実測波及131ページ）。7項目カード判定🟡（7日間隔に1日未達・07-23起点なら本来07-30解禁）をユーザーに提示し、「main merge」明示入力で1日前倒しマージ |
| **2026-07-29** | #370 | 汎用サイト構築・運営の教科書（docs/site-playbook.md・docs/site-build-checklist.md新規、波及0） |
| **2026-08-08** | #365 | LLMO 3/3: `index.html` グローバル用語集(DefinedTermSet)を正典化・e-Apostille追加。7項目カード判定🟢（直近の大型シグナル#319 07-30・#364 07-28がいずれも7日超過しバケツ空き）をユーザーに提示し「main merge」入力でマージ。実測波及132/134ページ、JSON-LDのみ・hreflang/canonical/title/価格の変更なしを全数確認 |
| **2026-08-08** | #397 | 他セッションPR：LTO現地レポート用WebP画像19点追加（#390分割）。実測波及0ページ（コード変更なし）。判定🟢 |
| **2026-08-08** | #391 | 他セッションPR：Apostille関連13ページの断定表現を条件付き表現へ修正（#390分割 batch1）。実測波及13ページ・想定通り一致、価格トークン変更なしを全数確認。判定🟢 |
| **2026-08-09** | #386 | e-Apostille帰化ページ（`/ja/e-apostille-fuka/`）の「紙原本＋物理アポスティーユが標準」を現行運用（紙のPSA原本 SECPA ＋ e-Apostille 電子認証）へ訂正。7項目カード判定🟢をユーザーに提示し「main merge」入力でマージ（squash `66e406a`）。**実測波及1ページ**（134 HTML中133ページ差分なし。生差分132件はJSバンドルハッシュ1行のみのノイズで、正規化して分離済み）。`prerender.ts`・`sitemap.xml`に触れるが変更は`description`1行＋`lastmod`1行のみで、hreflang/canonical/title/robots/routes/`_redirects`は全て無変更・sitemap URL数127で不変。JSON-LD 8件すべてパース成功。`dist`全体から「紙原本＋物理アポスティーユ」表記が1→0ページに |

**2026-08-09 〜 2026-08-23 のマージ（2026-08-23 に `git log origin/main` 実測で追記）**

| 日付 | PR | 内容 |
|------|----|------|
| 2026-08-12 | #409 | tracking Worker のデプロイワークフローを追加（旧A表 08-12 枠。混入9件は解消済みでマージ） |
| 2026-08-12 | #393 | 会社登記情報と外部サービス開示を追加（旧A表 08-10 枠） |
| 2026-08-12 | #399 | 提出要件の断定表現を修正（batch 2、旧A表 08-11 枠） |
| 2026-08-13 | #413 | title/desc不一致監査＋旧月ハードコード修正5ページ |
| 2026-08-15 | #408 | 顧客向け進捗確認ポータル＋書類アップロード（旧B表 09-05 枠を前倒し） |
| 2026-08-15 | #402 | Slack問い合わせ通知を現mainから再構築 |
| 2026-08-15 | #417 | LCCMとPSA電子版・紙原本の案内を更新 |
| 2026-08-16 | #419 | 行政・領事手続きの確認・問い合わせサポートを追加 |
| 2026-08-16 | #421 | 問い合わせ台帳GASの実台帳対応＋`/ja/nbi-koyukigen/` の title/desc 訂正 |
| 2026-08-18 | #422 | PSA料金と認証案内の訂正 |
| 2026-08-20 | #424 | tracking 書類アップロードのセキュリティ修正 |
| 2026-08-21 | #431 / #432 / #433 | 管理画面ログインの修正3本（最終的に #433 で誤認を招くログイン面を削除） |
| 2026-08-21 | #430 | 有料の取得可否調査プランを追加 |
| 2026-08-21 | #423 | CLAUDE.md に「PRの自動監視をしない」ルールを追加 |
| 2026-08-21 | #400 | 関連ページ見出しを言語別表示（i18n） |
| 2026-08-21 | #401 | 存在しない hreflang マッピングを削除 |
| 2026-08-23 | #435 | フォーム送信後に LINE/WhatsApp へ誘導 |

**⚠️ 2026-08-20〜08-21 運用逸脱の記録（2026-08-23 実測）**: この2日間に **PR番号を伴わない直接 push が12コミット** main に入っている（`temp`・`fix: specify zone for tracking API route`・`feat: protect tracking admin access and expire customer links`・AI下書き関連6本ほか）。PRを経ていないため7項目カードによる事前チェック（波及数実測・混入チェック・ローリング7日バケツ）が一度も適用されていない。内容は Worker / GAS 側（`dist` 非出力）が中心でSEO波及は小さいと見られるが、**手順としては本ファイルと CLAUDE.md の運用外**。今後は Worker/GAS のみの変更であってもPR経由に戻すこと。

**⚠️ 2026-07-19 運用逸脱の記録**: 同日に構造ファイル追記2件（#334・#335）と共通コンポーネント変更1件（#356: Hero.tsx/Pricing.tsx/DiyRisks.tsx等6ファイル軽微修正）が重なった。個別PRの7項目チェックはそれぞれ🟢だったが、「本日の累積」を見落としたローリング14日バケツ違反。**この日を起点に構造ファイル・共通コンポーネントの新規マージは本来2026-08-02まで凍結の予定だったが、07-23に#374（Pricing.tsx・共通コンポーネント）をユーザー承認の上で緊急マージしたため、ローリング14日バケツの起点が07-23に更新された。次の構造/共通コンポーネント枠は2026-08-06以降**（下記「残りスケジュール」の日付を+4日で更新済み）。

## クローズ済み

| PR | 理由 |
|----|------|
| #218 | 151ファイルの古い地雷（全共通コンポーネント＋構造を巻き込む）。韓国語i18nが必要なら現mainから新規ブランチで作り直す |
| #291〜#299 | dependabot 9本。#273 revert で dependabot.yml が消え前提消滅 |
| #305 | #319/#340 に分割済み（2026-07-18クローズ） |
| #339 | #357 に価格整合性部分のみ再分離して継続（2026-07-18クローズ、#356/#320巻き戻しを検出したため） |
| #350 | #354 に統合（2026-07-18クローズ） |
| #360 | #363/#364/#365 に波及数で3分割（2026-07-19クローズ） |
| #332・#353 | 本ファイルへ統合済み（旧docs予定表） |

**クローズ済み（2026-07-22）**: #358（07-18運用逸脱の記録）、#368（#366/#367追加）— いずれも本リライトと内容重複のためクローズ済み。

**クローズ済み（2026-07-25）**: #363（`docs/merge-schedule.md`の古いスナップショット混入・main競合のため#379としてリベース・再作成し#363はクローズ）。

### クローズ済み（2026-08-09 ユーザー承認・全26オープンPRのgit実測に基づく整理）

| PR | クローズ理由（実測） |
|----|---------------------|
| **#331** | **main と共通祖先がない**。`git merge-base origin/main pr/331` が空を返す。base の `3cf6413` はオブジェクトとして存在するが `origin/main` の祖先ではない（履歴が分岐・書き換わっている）。**リベースでは救えないため現mainから作り直しが必須**。内容（meta属性の未エスケープでdescription消滅＋OG画像404）は有効なので再作成すること。再作成版は`prerender.ts`変更のためB表扱い |
| **#361** | 後継の #408 と **21ファイル共有**。#408 が本PRを最新mainに追随させた版で、本PRは merge-base が **31コミット遅れ**。tracking は #408 に一本化 |
| **#388** | `mergeable_state: dirty`（#410/#411が同ファイルを先に更新）。内容は #410/#411/#413 で反映済み。固有だった「#354クローズ記録」は本ファイルに取り込み済み |
| **#406** | **69ファイル・9コミット遅れ・main と競合**。触る69ファイルのうち **8ファイルは merge-base 以降に main が更新済み**＝マージすると #386・#391 等を巻き戻すリスク。#415と14ファイル/#399と6/#394と5/#402と3が重複。#339・#354 と同じ「古い大型PR」パターン。**現mainから用途ごとに小さく切り直すこと** |
| **#389** | **価格方針の変更（2026-08-09 ユーザー決定）**。本PRのパック **¥55,000** に対し #415 が `lib/pricing.ts` で digitalPair **¥69,800**/paperPair **¥79,800**/assistedPair **¥94,800** を導入し、同一商品に2価格が存在していた。**#415 の新価格モデルを採用**することになったため本PRはクローズ。07-31 の ¥55,000 決定は取り消し扱い。なお本PRに含まれていた「`pages/PricingJa.tsx` の『IGRSが取り扱うのは物理アポスティーユ（紙原本）のみ』の是正」は **#415 が同じ箇所を置換済み**（#415適用後は当該誤記0件）と実測確認したため、別途の切り出しは不要 |

### クローズ済み（2026-08-23 実測で判明・いずれも未マージのままクローズ）

`git log origin/main` にも `list_pull_requests state=open` にも存在しないため、マージされずクローズされたと確定。

| PR | 旧B表での位置づけ | 未マージであることの影響 |
|----|-------------------|--------------------------|
| **#415** | 08-15 枠・「**採用が決定した新価格モデル**」（`lib/pricing.ts` 新設で価格を一元化） | **下記 C表の前提が崩れている。** `lib/pricing.ts` は main に存在せず（2026-08-23 実測）、価格の単一ソース化は**未実装**。C表の価格テーブルはどこにも実装されていない「決定だけされた値」である |
| **#414** | 08-22 枠・English homepage conversion flow | 旧C表が「`Hero.tsx` EN の `retrieve physical PSA originals` 残存は #414 の中で解消される」としていたが、**解消されていない**。要再確認 |
| **#362** | 08-29 枠・PSA e-Apostille事実訂正をトップ3ページへ反映 | 反映されないまま。内容の要否を現 main に対して再判定すること |

## 現在オープンなPR — 分類（2026-08-23 実測で全面再構成）

> **測定方法**（推測を排するため全PRで同一手順）: 各PRの `merge-base(origin/main, PR)` を取り、①PR側変更ファイル数、②うち merge-base 以降に main も更新済み＝**巻き戻しリスクのあるファイル**、③`git merge` の競合有無、④`npm run build` の成否、⑤`dist` の全HTMLを **JS/CSSバンドルのハッシュ名だけ正規化**して main のビルド結果と突き合わせた**実測波及ページ数**、を測定した。⑤の正規化を入れないと全PRが「波及135」に見える（バンドル名1行のノイズ）ので必須。
>
> **⑤は本ファイル「客観的に測る」節の Python 法と、シェルの `sed -E 's#...#...#g'`（区切り文字を `|` ではなく `#` にして `(js|css)` との衝突を回避したもの）の2通りで独立に測り、`#405`・`#387`・`#396` の3件で結果が一致することを確認済み**（いずれも既存ページ波及0）。「波及0」は誤測定と見分けがつきにくいため、0が出たPRは必ず2通りで突き合わせること。

### 実測サマリ（2026-08-23・main = `9cd4368`）

| PR | 内容 | main遅れ | 変更 | 巻き戻しリスク | 競合 | build | **実測波及** | レバー | 判定 |
|----|------|---------|------|---------------|------|-------|-------------|--------|------|
| **#405** | `/en/prc-professional-documents/` 新規追加 | 40 | 3 | 2 (App/prerender) | なし | ✅ | **既存0・新規1** | 構造ファイル追記のみ | 🟢 |
| **#387** | `/ja/prc-apostille/` 新規追加 | 40 | 4 | 3 (App/prerender/sitemap) | なし | ✅ | **既存0・新規1** | 構造ファイル追記のみ | 🟢 |
| **#398** | Hero/LTO画像をWebP配信へ | 40 | 3 | 0 | なし | ✅ | **4**（en・ja トップ、LTO報告2） | 個別ページ相当 | 🟢（下記の指摘つき） |
| **#396** | 未知URLの soft 404 を解消（`_redirects` のSPAフォールバック削除） | 40 | 1 | 0 | なし | ✅ | **0**（HTML出力に影響しない） | **構造（レベル2）** | 🟡 |
| **#403** | 共通UX修正（比較表の横スクロール注記・Navbar折返し・ServicePacks） | 40 | 3 | 0 | なし | ✅ | **126**（en 69 / ja 57） | **共通コンポーネント** | 🟡（下記の指摘つき） |
| **#394** | 必須項目のネイティブ検証を復旧（Footer の `noValidate` 削除） | 40 | 6 | 3 (Contact 3言語) | なし | ✅ | **134**（en 69 / ja 57 / ko 8） | **共通コンポーネント** | 🟡 |
| **#395** | Analytics同意バナー追加 | 40 | 5 | 3 (index.html/HomeEn/HomeJa) | **あり**（HomeEn.tsx） | 未測定 | 未測定 | 構造（`index.html`）＋共通 | 🔴 要リベース |
| **#418** | 会社情報のプライバシー配慮 | 26 | 18 | **9** | **あり**（App.tsx） | 未測定 | 未測定 | 構造＋共通＋18ファイル | 🔴 要リベース |
| **#425** | D1上の統合CRM | 19 | 6 | 2 (schema.sql/wrangler.toml) | **あり**（wrangler.toml） | 未測定 | 0想定（`dist`非出力） | Worker のみ | 🔴 要リベース |
| **#434** | 管理UIを Cloudflare Access の背後に復旧 | **1** | 5 | **0** | なし | 未測定 | 0想定（`dist`非出力） | Worker＋静的ツール | 🟢寄り・要外部設定 |

### A. 自動可キュー（🟢・個別ページ／新規追加のみ・日次1本ペース）

| 予定日 | PR | 内容 | 実測波及 | 備考 |
|--------|-----|------|---------|------|
| 未定 | **#405** | `/en/prc-professional-documents/` 新規追加 | 既存0・新規1ページ | 既存ページのHTMLが1バイトも変わらないことを実測確認。`App.tsx`/`prerender.ts` に触れるが**ルート追記のみ**。マージ前に最新 main へリベースして再測定すること |
| 未定 | **#387** | `/ja/prc-apostille/` 新規追加 | 既存0・新規1ページ | 同上。`sitemap.xml` は +1 URL |
| 未定 | **#398** | Hero/LTO画像をWebP配信へ | 4ページ | 下記の指摘を解消してから |

> **#398 への指摘（2026-08-23 実測）**: main の `Hero.tsx` は既に `<picture><source srcSet="/hero-photo.webp" type="image/webp"><img src="/hero-photo.png">` の形になっており、**WebP配信は既に効いている**。本PRが変えるのは `<img>` 側の `src` を `.png` → `.webp` にする点だけで、これは `<picture>` のフォールバック（WebP非対応環境向けの受け皿）を潰す変更にあたる。実利は「WebP非対応時に画像が出ない」リスクとの引き換えでほぼゼロ。**`Hero.tsx` の2行は落とし、LTO 2ページ分だけ残す**のが妥当（その場合の波及は2ページ）。

- 「自動可」= 期日に Routine が当日 `origin/main` で再検証し、🟢 の時だけ自動マージ（squash）。🟡🔴・競合・混入・build失敗は自動マージせず通知→手動へ切替。
- 上記は同日に複数マージしない。
- **予定日は未定のまま**にしてある。旧表の日付（08-10〜09-05）は既に過ぎており、日付だけ残すと「期限切れ」に見えて判断を誤らせるため。着手時にユーザーと決めること。

### B. 手動キュー（🟡🔴・構造ファイル／共通コンポーネント＝ローリング7日バケツ対象・単独デプロイ）

**ローリング7日バケツの起点（2026-08-23 時点）**: 直近の「大きい注ぎ」は **2026-08-21 の #400/#401**（`RelatedLinks` 系＋hreflang マッピング削除＝構造）。**次の構造／共通コンポーネント枠は 2026-08-28 以降**。

| PR | 内容 | 実測波及 / レバー | 実行条件 |
|-----|------|------------------|---------|
| **#396** | 未知URLの soft 404 を解消（`_redirects` の `/*  /en/index.html  200` 等3行を削除） | HTML波及0だが**レベル2（構造）** | 手動・単独。**安全性は実測済み**（下記）。バケツ起点 08-28 以降 |
| **#403** | 比較表の横スクロール注記・Navbar折返し・ServicePacks リンク修正 | **126ページ**（共通コンポーネント） | 手動・単独。**下記の分割を強く推奨**。なお **ServicePacks のリンク修正は本PR #436 で先行して取り込み済み**のため、#403 側は残り2点（比較表・Navbar）のみが実質の中身 |
| **#394** | Footer の `noValidate` を削除しネイティブ検証を復旧 | **134ページ**（共通コンポーネント・全言語） | 手動・単独。`ContactEn/Ja/Ko.tsx` は main が更新済みのためリベース必須 |
| **#395** | Analytics同意バナー追加 | 未測定・`index.html`（構造）＋ Home 2ページ | **`pages/HomeEn.tsx` で競合**。現 main からのリベースが先。同意バナーは全ページのCWVとGA計測に影響するため単独デプロイ |
| **#418** | 会社情報のプライバシー配慮 | 未測定・18ファイル | **`App.tsx` で競合**＋**PR側18ファイル中9ファイルを main が更新済み**＝そのまま入れると #400/#401/#433 等を巻き戻すリスク。#406 と同じ「古い大型PR」パターン。**現 main から小さく切り直すことを推奨** |
| **#425** | D1上の統合CRM | `dist` 非出力（SEO波及0） | **`workers/tracking/wrangler.toml` で競合**（main の #424/#432/直接pushが先行）。要リベース。Cloudflare 側の手動作業が前提 |
| **#434** | 管理UIを Cloudflare Access の背後に復旧 | `dist` 非出力（SEO波及0） | main遅れ1コミット・競合なし・巻き戻しリスク0 と**最も健全**。ただし **Cloudflare Access のポリシー設定（ユーザー側の手動作業）が前提**のため、設定完了後に随時マージ可（7日スペーシング対象外） |

> **#396 の安全性確認（2026-08-23 実測）**: SPAフォールバックを消すと「静的HTMLが無いルートが404になる」のが唯一の危険。`App.tsx` のページルート137件と `dist` の生成135件を突き合わせたところ、HTMLが無いのは**旧ルート13件のみ**（`/kika-shinsei-guide/`・`/nbi-clearance-guide/` 等）で、**13件すべて `public/_redirects` に 301 が定義済み**＝エッジで301が先に返るためフォールバック不要。逆に生成されているが `<Route path="...">` の直書きが無い11件は国別ページ（`COUNTRY_CONFIGS` から動的生成）で、静的HTMLがあるため影響なし。**SPAフォールバックに依存して 200 を返していた唯一の内部リンク `/ja/kikka-shinsei/` は本PR #436 で修正済み**（修正後、`dist` の全内部リンクを機械照合して解決しないリンク0件を確認）。したがって #396 を入れてもソフト404が素の404に変わるページはもう無い。

> **#403 の分割推奨（2026-08-23 実測）**: 波及126ページのうち **EN 69ページは `class` 属性の末尾に半角スペースが1つ増えるだけ**の差分（`${isJa ? 'whitespace-nowrap' : ''}` が EN で空文字になり `tracking-tight "` になる）。表示は一切変わらないのに Google には「69ページ更新」シグナルとして届く。**テンプレートリテラルを `${isJa ? ' whitespace-nowrap' : ''}`（スペースを中に入れる）に直すだけで波及が 126 → 57 に減る。** さらに `ServicePacks.tsx` のリンク修正（下記の実害バグ）だけを切り出せば**波及1ページ（`/ja/` のみ）の 🟢 PR** になり、即日投入できる。title / canonical / hreflang は EN・JA とも無変更を確認済み。

### C. 価格方針 — **⚠️ 2026-08-09 の決定は未実装（2026-08-23 実測）**

**下表は `lib/pricing.ts` を単一の正とする方針だったが、その `lib/pricing.ts` を導入する #415 が未マージのままクローズされたため、main には `lib/pricing.ts` が存在せず、下表の価格はどこにも実装されていない。** 方針を再決定するか、#415 の価格一元化部分を現 main から作り直す必要がある。

| 項目 | 値（#415 で予定されていた `lib/pricing.ts`） | main での実装 |
|------|------------------------|--------------|
| inputSupport | ¥22,000 | 未実装 |
| digitalSingle | ¥39,800 | 未実装 |
| digitalPair | ¥69,800 | 未実装 |
| paperPair | ¥79,800 | 未実装 |
| assistedPair | ¥94,800 | 未実装 |
| additionalDigitalDocument | ¥29,800 | 未実装 |
| consolidatedPaperDelivery | ¥11,000 | 未実装 |
| apostilleOnly / nbiApostille | ¥39,000 | 未実装 |
| lto | ¥116,000 | 未実装 |
| naturalization | ¥132,500 | 未実装 |

## ✅ 本番 main に現存していた不整合 — 本PR（#436）で解消済み（2026-08-23）

予定表の突合中に、オープンPRの問題ではなく **いま公開されている main 自体のバグ** が見つかったため、本PRで修正した。以下は経緯の記録。

### 1. 価格が商品ごとに複数に割れていた → 統一済み

| 商品 | 修正前（割れていた状態） | 修正後 |
|------|--------------------------|--------|
| 国際結婚準備パック | `Hero.tsx` **¥99,800（税送料別）** / `Pricing.tsx` **¥94,000（税込DHL込み）** / JAトップ JSON-LD **¥99,800** | **¥88,000（税込・送料込み）** に3ヶ所とも統一 |
| 配偶者ビザ準備書類パック | ¥94,000 | **¥88,000（税込・送料込み）** |
| LTO 外免切替 | `Pricing.tsx` **¥116,000** / 個人・法人各ページ **¥100,000（税抜・送料別）** / JSON-LD **¥100,000** | **¥99,000（税込・送料込み）** に全ページ統一 |
| NBI＋DFAアポスティーユ | `/ja/nbi-clearance/` は **¥55,000＋アポ¥20,000＋郵送¥6,000＝¥81,000** の積み上げ、JAトップ JSON-LD は **¥55,000** で「取得代行＋アポスティーユ」 | **¥55,000（アポスティーユ・送料込みの総額）** に統一し、料金表に取得代行プランを新設 |

**原因（git 実測）**: `Hero.tsx` の ¥99,800 は 2026-05-13 に ¥90,000 へ値下げしたものが、**2026-05-27 の #261「Reduce mobile scroll fatigue」（モバイルUI改修）に巻き込まれて3月の価格へ巻き戻っていた**回帰。以後3ヶ月間、誰も気づかず本番に出ていた。JAトップの JSON-LD は 3 月から一度も更新されておらず、`dateModified` も `'2026-03-01'` のままだった。

**教訓**: UI改修PRが価格文字列を巻き戻しても、レビューでもCIでも検知されない。**価格は「触った覚えのないPR」で壊れる**ため、価格変更後は `grep` で全出現箇所を突き合わせること。根治は #415 で予定されていた `lib/pricing.ts` の単一ソース化だが、#415 は未マージのままクローズされている（上記C表参照）。

### 2. JAトップページの壊れた内部リンク → 修正済み

`components/ServicePacks.tsx` の「帰化申請書類パック」カードが `/ja/kikka-shinsei/`（`kikka` = タイプミス）を指しており、ルートもリダイレクトも存在しないため `_redirects` の SPA フォールバックに拾われて **HTTP 200 のソフト404** を返していた。`/ja/kika-shinsei-guide/` へ修正済み（波及1ページ）。

修正後、`dist` の全 HTML の内部 `href` を「生成済みページ＋`_redirects` 左辺＋静的アセット」と機械照合したところ、**解決しない内部リンクは0件**。

### 3. 全135ページから 404 していた `apple-touch-icon.png` → 追加済み

上記のリンク照合で判明。`index.html` が全ページで `<link rel="apple-touch-icon" href="/apple-touch-icon.png">` を出しているが、**ファイルが一度も存在していなかった**（iOS がホーム画面追加時などに取得を試みて 404）。`public/favicon.svg` から 180×180 の PNG を生成して `public/` に追加。**HTML を一切変更しないため波及0ページ**。

### 残っている不整合（PSA の価格が保留のため未着手）

- JAトップ JSON-LD の `minPrice: '50000'`「CENOMAR・PSA出生証明書取得代行＋DFAアポスティーユ」— **対応する料金プランが料金表に存在しない**。加えて `Pricing.tsx` は「PSA民事書類はe-Apostille〈電子〉のみ」と明記しており、**「DFAアポスティーユ」という表記自体が現行運用と食い違う**（#356・#374・#386 と同じ「提供不可能なサービス表示」の系統）。
- `/ja/business/kigyou/` の「PSA出生証明書 + DFAアポスティーユ ¥55,000（税抜）」— 同上。
- 料金ページの「Updated: 2026年5月1日」— `lib/seoDate.ts`（構造ファイル）由来のため、変更には別途の明示許可が必要。

## 注意事項

- **main merge は 2026-07-15 まで禁止**（CLAUDE.md）。以降も解除条件は継続し、ユーザーが「main merge」と入力した時のみ有効
- 🟢 は本来「コンテンツ自由・日次OK」。🟡🔴 を触った後のみ 14日観察（構造・一括・共通コンポーネントが対象、かつ同一ローリング窓を共有）
- 古いブランチは **マージ前に origin/main へリベースし、競合＆重複ブロックを解消・`npm run build` 確認**すること
- **受注を上げる最速手は SEO ではなくコンバージョン改善**（CTA・価格・納期・信頼要素）。構造非依存・即効・リスクゼロなので、上記スケジュールと無関係にいつでも実施可
- 期日が来た行は、マージしたら「完了済み」表へ移動する

### デプロイ確認の落とし穴（2026-08-09 調査で判明）

- **Cloudflare Pages のデプロイは GitHub Actions には出ない（Checks には出る）**。`.github/workflows/` にあるのは `gsc-monitor.yml`・`hreflang-check.yml`・`seo-gate.yml` の3つだけで、サイトのデプロイは Cloudflare の Git 連携が push を検知して自前でビルドしている。ただし Cloudflare Pages の GitHub App は **`Cloudflare Pages` という名前の check run** を出すため、**Actionsタブではなく Checks（PRのチェック一覧）を見ること**。PRではプレビューデプロイの成否がここで確認でき、BotがプレビューURL付きのコメントも残す（#413 で実測）。ビルドログ本体は Cloudflare ダッシュボード（check run の details_url）にある。
- **`hreflang Post-Deploy Check` の緑チェックは「hreflang が正常」の証明にならない**。`hreflang-check.yml` の検証ステップに `continue-on-error: true` が付いているため、**hreflang が全削除されていてもジョブは success で終わり**、メール通知が飛ぶだけ。Actions一覧の ✅ は「デプロイが完了した」ことしか意味しない。実際の合否は **ジョブのステップ単位**（`Run hreflang check` の conclusion と `Send alert if hreflang broken` が skipped か）で確認するか、`python scripts/check_hreflang.py` を手元から本番に対して実行して確かめること。
- **マージ前フックの文言が古い**。フックは「ローリング**14日**バケツ」と表示するが、#378（2026-07-25・ユーザー承認）で**7日**に短縮済み。正典は本ファイルと CLAUDE.md であり、フックの表示ではない。
- **docs のみのPRでも Cloudflare のビルドは毎回走る**（ビルド出力は変わらないためGoogleへの更新シグナルにはならない）。2026-08-08 は12分間に5回 push され、ビルドも5本連続で走った。CLAUDE.md の「デプロイは1日1回まで」からの逸脱にあたるため、**docs 更新は複数件をまとめて1回の push にすること**。
